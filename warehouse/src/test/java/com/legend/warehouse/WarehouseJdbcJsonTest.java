package com.legend.warehouse;

/**
 * The same differential with the driver on JSON ({@code resultFormat=json}): both formats a client can
 * choose give DuckDB's driver's values. The base class runs it on Arrow, the driver's default.
 */
class WarehouseJdbcJsonTest extends WarehouseJdbcTest {
}
