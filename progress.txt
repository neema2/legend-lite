Typed AST Big-Bang — Phase A in flight

PLAN (frozen):
  ~/.windsurf/plans/typed-ast-bigbang-mechanical-c0954a.md

DECISIONS (locked):
  1. Pure typed HIR (no sidecar map) — CompiledExpression(TypedSpec hir, deps).
  2. Distinct control-flow variants (TypedIf/Let/Match/Cast/Zip/Write/Serialize).
  3. TypedUserCall(args, body) wraps inlined body.
  4. Phase A scope = compiler only; MappingResolver (B) + PlanGen (C) stay red.

DONE (this session):
  - 8 new TypedSpec variants added + wired to sealed permits list.
  - TypedSpec: added expressionType() alias + type()/schema()/multiplicity()/
    isRelation()/isScalar()/isMany() default methods.
  - Literal variants retyped to match AST value types
    (TypedCInteger: Number; TypedCDateTime/StrictDate/StrictTime: String).
  - CompiledExpression reshaped to pure HIR: (TypedSpec hir, CompiledDependencies).
  - TypeCheckEnv: compileExpr returns TypedSpec; lookupCompiled removed.
  - TypeChecker: typedSpecs/types sidecar map removed; lookupCompiled removed;
    compileExpr switch produces TypedSpec literal variants directly;
    check() returns CompiledExpression(root hir, deps).

FOLLOWUPS (tighten during Phase C when consumer shape is known):
  - TypedFrom.mapping / .runtime: currently TypedSpec; almost certainly want
    String FQN. Tighten once PlanGenerator reveals the consumer shape.
  - TypedNativeCall args: ColSpec params currently skipped from args list
    (no TypedSpec representation for column-name tokens yet). PlanGen must
    still read ColSpec names from AF.parameters(). Consider TypedColSpec or
    wrapping name as TypedCString when PlanGen migrates.
  - TypedSort keys: bare `sort(col)` with no key lambda emits empty keys
    list; collection-sort on identity has no carrier for the sort direction.
    Revisit when collection-sort consumer surfaces.

PROGRESS on checker migrations (check() returns TypedSpec):
  - Batch A (leaves): GetAll, TableReference, Tds                     [3/3 ✅]
  - Batch B (passthrough): Filter/Slicing/Rename/Select/Distinct/
    Concatenate/Cast/TypeConversion/Get/Flatten/From                  [11/11 ✅]
  - Batch C (control flow): If, Let, Match, New                       [4/4 ✅]
  - Batch D (scalar/IO/single-lambda): Write, Serialize, Map,
    Fold, Sort, Scalar                                                [6/6 ✅]
  - Batch E (structural): Extend, Project, GroupBy, Aggregate,
    Pivot, Join, AsOfJoin, Eval, GraphFetch, Zip, Function            [0/11 🔴]

REMAINING for Phase A (~2-3h focused sprint):
  - TypeChecker private methods still return TypeInfo; rewrite each to return
    the matching TypedSpec variant:
      compileFunction    -> TypedSpec (per-af kind: native/user/control-flow)
      compileLambda      -> TypedLambda
      compileVariable    -> TypedVariable
      compileProperty    -> TypedPropertyAccess
      compileCollection  -> TypedCollection
      compileNew         -> TypedNewInstance
      inlineUserFunction -> TypedUserCall
      resolvePackageableElement -> throw (not a standalone expr)
      inlineStructExtract -> TypedStructExtract
  - Delete dead helpers scalarTyped/scalarTypedMany/typed (no callers after
    compileExpr flip; they still reference the removed `types` map).
  - AbstractChecker: compileLambda / compileLambdaArg / buildTypedLambda return
    TypedSpec / TypedLambda.
  - 36 checkers: each check(...) returns TypedSpec; build the matching Typed*
    variant at its single builder site; read child typed specs from
    env.compileExpr return value (thread through; no lookup by VS key).
  - Delete TypeInfo.java at the very end.

REMAINING for Phase B (MappingResolver) — ~5 variant-read sites.
REMAINING for Phase C (PlanGenerator) — ~35 variant-read sites.

TESTS: expected red during Phase A; re-enable after B+C.
