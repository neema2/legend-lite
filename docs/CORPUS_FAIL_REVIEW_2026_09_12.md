# DuckDB corpus fail roster — row-by-row review (2026-09-12, homework done)

121 rows on `duckdb-fail-roster.txt` after `885c9d7eb` (108 before the 4.145.0 bump: +14 new upstream tests; −1 on the flag leg; the two flag tests and the two substr goldens returned since). Every row is listed once. Verdicts come from the test BODIES read at the pinned checkout (the assert form: rows, SQL text, plan text, or an engine-internal call), not from the refusal text alone; where the refusal was a bare `Assert failed` the failing assert is named.

## BURN — a semantic gap a user could hit; a leg (bodies read, cause named) — 8

| test | refusal (exact, trimmed) | what the test asserts / verdict |
|---|---|---|
| `executionPlan::tests::testExecutionPlanGenerationForLambdaFromWithEnumMapping` | AssertFailed: Assert failed | BURNED 2026-09-12: the engine's mapping-less executionPlan overload adds PUSH_DOWN_ENUM_TRANSFORM before routing; the plan channel now honours the flag (the decode stays in the SQL, the tuple carries no enumeration-mapping id) and the text channel spells the pushed-down decode the engine's flat way (`col in (a, b)` / `col = a`, `else null`) |
| `meta::relational::graphFetch::tests::simple::testCheckedWithCircularConstraints` | AssertFailed: assertJsonStringsEqual: FIRST DIFF at $[2].defects expected 1 element(s), got 0 | checked graphFetch (graphFetchChecked) with class constraints: the engine reports a constraint defect at $[2]; ours reports none — constraint evaluation on checked fetch |
| `advanced::forced::structure::testQualifierWithOperation` (DESIGN) | AssertFailed: assertEquals (sql-text ROW verdict — golden rows vs ours diverged, whatever the text said): h2-a | READ (SQL dumped): the engine LEFT JOINs the qualifier's correlated subselect and concat() ignores the NULL, so every firm yields a row ('Test' where no employee qualifies — 4 rows; Pure's variadic plus agrees: [] + 'Test' = 'Test'); ours INNER JOINs and keeps 1 — the qualifier's result rides the drop-empty operator lane (StoreLane: a [*] CALL result is a pure value, only a relation-row column read is SQL-lane). A qualified property's result IS a store read after resolution; deciding that provenance is the operator-run DESIGN rule, not a spot fix → DESIGN |
| `advanced::forced::structure::testTwoQualifiersWithOperation` (DESIGN) | AssertFailed: assertEquals (sql-text ROW verdict — golden rows vs ours diverged, whatever the text said): h2-a | READ (SQL dumped): the engine LEFT JOINs the qualifier's correlated subselect and concat() ignores the NULL, so every firm yields a row ('Test' where no employee qualifies — 4 rows; Pure's variadic plus agrees: [] + 'Test' = 'Test'); ours INNER JOINs and keeps 1 — the qualifier's result rides the drop-empty operator lane (StoreLane: a [*] CALL result is a pure value, only a relation-row column read is SQL-lane). A qualified property's result IS a store read after resolution; deciding that provenance is the operator-run DESIGN rule, not a spot fix → DESIGN |
| `mapping::relation::testDateTimeInclusiveRangeQuery` (ACCEPTED DIVERGENCE, USER 2026-09-12) | AssertFailed: [settlementDateTime] (2 rows) \| is not equivalent to: \| [settlementDateTime] (1 rows) | READ: the engine writes every sub-second digit into the SQL (convertDateToSqlString `S*`) as a plain quoted string; H2 coerces that string to the column's TIMESTAMP precision before comparing, so .123456789 <= .123 comes out TRUE there. In Pure the comparison is FALSE; DuckDB agrees with Pure. MEASURED in the real legend-pure interpreter (5.99.0, PureTestBuilderInterpreted): `%…23.123456789 <= %…23.123` = false, compare = 1, the range filter over the two rows returns [2] only — Pure keeps ONE row, as we do; the engine's two rows are H2's string-literal coercion. USER 2026-09-12: ACCEPTED (keep Pure) — accepted-roster row, bucket engine-golden-defect:h2-literal-coercion |
| `mapping::relation::testMixedMappingWithFilterInProject` | NotImplementedException: a navigation join over this union demands key column 'firm_ID', which NO union member | union lane: a navigation join over a mixed (union) mapping demands key column firm_ID no member carries |
| `mapping::relation::testRelationStoreAccessorOnView` (DESIGN) | DataError: Catalog Error: Table with name personView does not exist! \| Did you mean "personWithAddressTable"? | a relation store accessor on a VIEW (#>{db.personView}#): the accessor lowers to a physical table by name; the view expansion (ViewRelation.viewRelationExpr) is coupled to a mapping's join-chain machinery — a mapping-free view relation is a DESIGN lane |
| `milestoning::businessdate::testBusinessDateInjectionFromVarReferenceInProjectUsingExternalFunction` (ACCEPTED DIVERGENCE, USER 2026-09-12) | AssertFailed: assertSameSQL (sql-text ROW verdict — golden rows vs ours diverged, whatever the text said): h2- | READ (SQL dumped): filterOrders(o) = o->filter(product(orderDate).type == 'STOCK')->map(id). The engine's golden projects `"root".id` UNCONDITIONALLY, left-joining the filtered subselect it never reads — order 1 (product not STOCK) still yields 1. Ours projects CASE WHEN type = 'STOCK' THEN id ELSE NULL over the same temporal join → null for order 1, which is Pure's answer (filter then map: empty → null cell). MEASURED in the real legend-pure interpreter: `$o->filter(x | $x.second == 'STOCK')->map(x | $x.first)` over one non-STOCK pair = [] and the projected cell for that order is EMPTY — Pure gives the empty cell, as we do; the engine's `"root".id` is its compiler projecting the unfiltered root. USER 2026-09-12: ACCEPTED (keep Pure) — accepted-roster row, bucket engine-golden-defect:unfiltered-root-projection |

## BURN candidates — cause named, one probe outstanding — 3

| test | refusal (exact, trimmed) | what the test asserts / verdict |
|---|---|---|
| `alloy::connections::tests::relationalMapper::testRelationalMapperTwoDBs` | AssertFailed: expected: 'select "root".NAME as "name", "synonymtable_0".NAME as "cusip" from snDB.productSchem | RelationalMapper (db/schema/table renaming declared on the connection): SQL text with the remapped names — a user-facing connection feature; the text is H2-style and our text channel can meet it |
| `alloy::connections::tests::relationalMapper::testRelationalMapperWithJoin` | AssertFailed: expected: 'select "addresstable_0".NAME as "address" from snDBDefault.default.firmTableNew as "r | RelationalMapper (db/schema/table renaming declared on the connection): SQL text with the remapped names — a user-facing connection feature; the text is H2-style and our text channel can meet it |
| `meta::relational::graphFetch::tests::union::propertyLevel::test6` (DESIGN) | AssertFailed: assertJsonStringsEqual: FIRST DIFF at $[0].legalName expected Firm B, got Firm X | ORDER, not data (probed): every firm's JSON matches; ours serializes in member declaration order (set1, set2, set3 — the ordered array aggregate over the negated serial), the engine gives set3, set1, set2 — the Firm mapping is a `special_union` (the engine's merge-by-join-name union), whose query shape sets that order: the inclusive-union DESIGN lane, not a serial fix |

## DESIGN — a lane, decided in a design session — 14

| test | refusal (exact, trimmed) | what the test asserts / verdict |
|---|---|---|
| `executionPlan::m2m2r::tests::planGraphFetchWithDerivedProperty` | NotImplementedException: class query under TypedGraphFetch is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `executionPlan::m2m2r::tests::planGraphFetchWithNestedDerivedProperty` | NotImplementedException: class query under TypedGraphFetch is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `executionPlan::tests::execution::testPureExecutionStrategyForCreateAndPopulateTempTableExecutionNode` | NotImplementedException: class query under TypedPropertyAccess is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `executionPlan::tests::execution::testPureExecutionStrategyForRelationalInstantiationExecutionNode` | NotImplementedException: class query under TypedPropertyAccess is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `executionPlan::tests::testModelConnectionJoin` | MappingResolutionException: class 'meta::pure::mapping::modelToModel::test::shared::src::_Person' is not mappe | model-to-model: class not mapped in the M2M mapping |
| `graphFetch::tests::XStore::inMemoryAndRelational::testCrossMappingJsonToDBWithExplosion` | MappingResolutionException: class 'meta::pure::graphFetch::tests::XStore::inMemoryAndRelational::T_Trade' is n | model-to-model: MNM explosion is a roadmap feature |
| `graphFetch::tests::XStore::inMemoryAndRelational::testCrossStoreWithCSVDataSource` | NotImplementedException: class query under TypedMap is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `lineage::scanColumns::test::testNonDataTypeProperty` | NotImplementedException: class query under TypedMap is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `meta::relational::tds::schema::tests::resolveSchemaTest` | type: no overload of 'meta::pure::tds::schema::tests::assertSchemaRoundTripEquality' matches 2 argument(s) of  | resolveSchema — PARKED (needs reflective equality, the stdlib surface) |
| `functions::sqlstring::testToSQLStringWithAbs` | NotImplementedException: class query under TypedNewInstance is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `functions::sqlstring::testToSQLStringWithAggregation` | NotImplementedException: class query under TypedNewInstance is not resolvable yet (H2 vocabulary) | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `json::testResultToJsonStream` | type: property 'rows' of 'meta::pure::tds::TabularDataSet': expected (), got meta::pure::tds::TDSRow (value: P | an in-memory TabularDataSet instance literal (^TabularDataSet(columns, rows)) streamed to JSON — the instances / code-as-data lane |
| `mapping::include::testStoreSubstitution` | NotImplementedException: class query under if() with a runtime condition is not resolvable yet | host-navigation vocabulary: class query under a graphFetch/property/map/new-instance/if — the instances lane |
| `mapping::modelJoin::advanced::testNestedModelJoinCompoundInnerCondition` | MappingResolutionException: association 'meta::relational::tests::mapping::modelJoin::domain::Person_Firm' is  | model join: association not mapped |

## OUT — engine machinery the platform replaces, text idioms of its printer/optimizer, unjudgeable goldens, walls by decision — 96

| test | refusal (exact, trimmed) | what the test asserts / verdict |
|---|---|---|
| `executionPlan::tests::datetime::testPlanWithLocalH2ConnectionWithSQL` | type: unknown function 'meta::protocols::pure::vX_X_X::transformation::fromPureGraph::executionPlan::transform | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `executionPlan::tests::inheritance` | NotImplementedException: plan: no class mapping for 'meta::relational::tests::model::inheritance::RoadVehicle' | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testCrossDbPlanGenerationWithRelationFromWithOnlyRuntimes` | NotImplementedException: executionPlan mapping argument must be a reference (or the query must carry ->from),  | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testEnumPushDownWithExternalFormat` | type: unknown function 'meta::external::format::shared::transformation::tests::exampleExternalFormatExtension' | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `executionPlan::tests::testGraphFetchH2TempTableStrategy` | NotImplementedException: instanceOf(meta::pure::graphFetch::executionPlan::StoreMappingGlobalGraphFetchExecuti | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `executionPlan::tests::testGraphFetchH2TempTableStrategyWithQuoteIdentifiers` | NotImplementedException: instanceOf(meta::pure::graphFetch::executionPlan::StoreMappingGlobalGraphFetchExecuti | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `executionPlan::tests::testGroupByWithOpenVariableInAgg` | AssertFailed: assertEqualsH2Compatible (sql-text, rows underivable — DataError: Catalog Error: Table with name | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testGroupByWithTwoOpenVariablesInAggAndFilter` | AssertFailed: assertEqualsH2Compatible (sql-text, rows underivable — DataError: Catalog Error: Table with name | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testModelConnectionDeepFunction` | NotImplementedException: plan: no class mapping for 'meta::pure::mapping::modelToModel::test::shared::dest::Pe | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testPlanForExecutionOption` | type: unknown function 'isExecutionOptionPresent' — no function of this name in the native or user catalog (un | engine plan/option machinery: isExecutionOptionPresent / functionReturnType / header |
| `executionPlan::tests::testPreprocessFunctionOnRuntime` | type: unknown function 'functionReturnType' — no function of this name in the native or user catalog (unported | engine plan/option machinery: isExecutionOptionPresent / functionReturnType / header |
| `executionPlan::tests::testQuoteIdentifiersFlagWithGraphFetch` | AssertFailed: assertEquals (sql-text, oracle declined: golden execution: Schema "productSchema" not found; SQL | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testRelationalProjectionWithExternalFormat` | type: unknown function 'meta::external::format::shared::transformation::tests::exampleExternalFormatExtension' | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `executionPlan::tests::testRoutingContextBuilderFunctions` | type: unknown function 'header' — no function of this name in the native or user catalog (unported platform fu | engine plan/option machinery: isExecutionOptionPresent / functionReturnType / header |
| `executionPlan::tests::testSupportStreamFlagWithGraphFetchAndFrom` | type: deferred let binding '$targetGraph' has no type outside a consuming call position (tree/colspec bindings | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testTemporalDateVariableInFunctionExpressionWithPropagation` | AssertFailed: assertEqualsH2Compatible (sql-text, rows underivable — DataError: Catalog Error: Table with name | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testTwoMappingsOneRuntime` | AssertFailed: expected: 'Relational\n(\n  type = TDS[(legalName, String, VARCHAR(200), ""), (legalNameSimple,  | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testTwoMappingsOneRuntimeWithoutExternalMapping` | AssertFailed: expected: 'Relational\n(\n  type = TDS[(legalName, String, VARCHAR(200), ""), (legalNameSimple,  | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::testViewToTDS` | NotImplementedException: planToString: no getAll or table root (multi-node plans pending) | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `executionPlan::tests::withPlatform` | DialectCapability: collection reduction 'STRING_AGG' reached a dialect without a list encoding | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `graphFetch::tests::XStore::milestoning::testCrossStoreGraphFetchWithRelationalDatePropagationForMilestonedPropertyConstraint` | type: unknown function 'meta::legend::compileLegendGrammar' — no function of this name in the native or user c | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `graphFetch::tests::XStore::milestoning::testCrossStoreGraphFetchWithRelationalDatePropagationForMilestonedPropertyZeroToOne` | type: unknown function 'meta::legend::compileLegendGrammar' — no function of this name in the native or user c | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `graphFetch::tests::m2m2r::milestoning::milestonedSourceToNonMilestonedTargetProperty::testFlatten_ViaNoArgMapping` | type: from() argument 1 must be a mapping or runtime reference, got TypedVariable | the mapping is COMPILED FROM A GRAMMAR STRING at runtime (getNoArgFlattenMapping builds '###Mapping …' text) — engine reflection, never a reference |
| `graphFetch::tests::m2m2r::milestoning::milestonedSourceToNonMilestonedTargetProperty::testFlatten_ViaNoArgMapping_ViaAssociation` | type: from() argument 1 must be a mapping or runtime reference, got TypedVariable | the mapping is COMPILED FROM A GRAMMAR STRING at runtime (getNoArgFlattenMapping builds '###Mapping …' text) — engine reflection, never a reference |
| `router::preeval::tests::testPrerouting42` | type: a name-less project column must be a property navigation (its leaf names the column); give explicit name | router pre-evaluation test (graphFetch results in lets pre-evaluated before routing) — the engine's router |
| `tds::toRelation::testJoinFunc` | TypeInferenceException: in function 'meta::pure::tds::toRelation::test': unknown function 'meta::pure::tds::to | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `tds::toRelation::testJoinUsing` | TypeInferenceException: in function 'meta::pure::tds::toRelation::test': unknown function 'meta::pure::tds::to | engine programs the platform does not carry: protocol transformPlan / compileLegendGrammar / exampleExternalFormatExtension / tds::toRelation::transform |
| `functions::sqlQueryToString::default::testProcessIdentifierWithQuoteChar` | type: walled body 'meta::relational::functions::sqlQueryToString::DbConfig$prop$selectSQLQueryProcessor': the  | walled printer body (DbConfig.selectSQLQueryProcessor / translation / post-processor / column rename) |
| `functions::sqlQueryToString::tests::testCaseNullOrderingSupportFlagOnSkipsCaseShim` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testCaseNullOrderingSupportUsesUnderlyingExpression` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testNativeOnlySupportIgnoresExplicitNullOrder` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testNullsHighOrderingSupport` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testNullsHighOrderingSupportFlagOn` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testNullsLowOrderingSupport` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testNullsLowOrderingSupportFlagOn` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testTempTableSqlStatementsForH2` | NotImplementedException: store resolution left user call 'meta::relational::functions::sqlQueryToString::DbCon | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `functions::sqlQueryToString::tests::testUniformNullsLastOrderingSupport` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testUniformNullsLastOrderingSupportFlagOn` | type: walled body 'meta::relational::functions::sqlQueryToString::NullOrderingSupport$prop$processSortItem': t | walled printer body: NullOrderingSupport.processSortItem (the engine's SQL printer) |
| `functions::sqlQueryToString::tests::testUseDbNativeImplicitNullOrderingResolutionConnectionDisabledIsInert` | TypeInferenceException: in function 'meta::relational::functions::sqlQueryToString::useDbNativeImplicitNullOrd | the printer's own flag resolution (contextHasFlag inside useDbNativeImplicitNullOrdering); the flag is loud in the platform |
| `functions::sqlQueryToString::tests::testUseDbNativeImplicitNullOrderingResolutionConnectionEnabled` | TypeInferenceException: in function 'meta::relational::functions::sqlQueryToString::useDbNativeImplicitNullOrd | the printer's own flag resolution (contextHasFlag inside useDbNativeImplicitNullOrdering); the flag is loud in the platform |
| `functions::sqlQueryToString::tests::testUseDbNativeImplicitNullOrderingResolutionExeCtxFlag` | TypeInferenceException: in function 'meta::relational::functions::sqlQueryToString::useDbNativeImplicitNullOrd | the printer's own flag resolution (contextHasFlag inside useDbNativeImplicitNullOrdering); the flag is loud in the platform |
| `functions::sqlQueryToString::tests::testUseDbNativeImplicitNullOrderingResolutionOffByDefault` | TypeInferenceException: in function 'meta::relational::functions::sqlQueryToString::useDbNativeImplicitNullOrd | the printer's own flag resolution (contextHasFlag inside useDbNativeImplicitNullOrdering); the flag is loud in the platform |
| `functions::sqlQueryToString::tests::testUseDbNativeImplicitNullOrderingResolutionOrPrecedence` | TypeInferenceException: in function 'meta::relational::functions::sqlQueryToString::useDbNativeImplicitNullOrd | the printer's own flag resolution (contextHasFlag inside useDbNativeImplicitNullOrdering); the flag is loud in the platform |
| `functions::toPostgresModel::tests::testConvertJoinTreeNode` | TypeInferenceException: in function 'meta::relational::functions::toPostgresModel::convertSemiStructuredArrayF | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `functions::toPostgresModel::tests::testConvertSelectSQLQuery` | TypeInferenceException: in function 'meta::relational::functions::toPostgresModel::convertSemiStructuredArrayF | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::graphFetch::domain::tests::testGraphFetch` | TypeInferenceException: in function 'meta::pure::graphFetch::domain::extractDomainTypeClassFromGraphFetchTree' | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::graphFetch::tests::milestoning::testMilestonedProperty` | AssertFailed: expected: 'PureExp\n(\n  type = String\n  expression =  -> serialize(#{meta::relational::tests:: | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `meta::relational::metamodel::execute::tests::testConnectionEqualityAllButOnePropertySame` | NotImplementedException: scalar match: the arm collection has a non-literal prefix (extension-contributed arms | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::metamodel::execute::tests::testConnectionEqualityAllSameStatic` | NotImplementedException: scalar match: the arm collection has a non-literal prefix (extension-contributed arms | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::metamodel::execute::tests::testConnectionEqualityTypeDiff` | NotImplementedException: scalar match: the arm collection has a non-literal prefix (extension-contributed arms | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::metamodel::execute::tests::testConnectionEqualityTypeSameSpecDiff` | NotImplementedException: scalar match: the arm collection has a non-literal prefix (extension-contributed arms | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::metamodel::execute::tests::testConnectionEqualityTypeSpecSameAuthDiff` | NotImplementedException: scalar match: the arm collection has a non-literal prefix (extension-contributed arms | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::testDataGeneration::tests::alloy::testAlloyTestDatGenWithQuotedColumnsForViews` | NotImplementedException: testDataGen: view-backed relation 'AltID_View' — view slice pending | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `advanced::resultSourcing::relationalResultSourcingOfListExecutionPlan` | NotImplementedException: plan: computed scalar projection spelling pending | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `ddl::dropAndCreateTempTable` | IllegalStateException: a scalar query has no row scope for $colsAsString.null | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `ddl::testCreateTempTableStatement` | NotImplementedException: scalar match: no arm statically accepts input type meta::relational::metamodel::datat | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `functions::pureToSqlQuery::addDriverTablePkForProject` | type: unknown function 'routeFunction' — no function of this name in the native or user catalog (unported plat | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `functions::pureToSqlQuery::simpleFunctionExpressionTranslationAdjust` | type: walled body 'meta::relational::functions::sqlQueryToString::sqlQueryToString': the engine's SQL printer  | walled printer body (DbConfig.selectSQLQueryProcessor / translation / post-processor / column rename) |
| `functions::pureToSqlQuery::simpleFunctionExpressionTranslationNow` | type: walled body 'meta::relational::functions::sqlQueryToString::sqlQueryToString': the engine's SQL printer  | walled printer body (DbConfig.selectSQLQueryProcessor / translation / post-processor / column rename) |
| `functions::pureToSqlQuery::testFindAliasMappingBySchemaName` | type: unknown function 'relation' — no function of this name in the native or user catalog (unported platform  | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `functions::pureToSqlQuery::testFindFunctionSequenceMultiplicity` | TypeInferenceException: in function 'meta::relational::functions::pureToSqlQuery::findFunctionSequenceMultipli | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `functions::pureToSqlQuery::testImportDataFlow` | type: unknown function 'newMultiValueMap' — no function of this name in the native or user catalog (unported p | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `functions::pureToSqlQuery::testMergeOldAliasToNewAlias` | TypeInferenceException: in function 'meta::relational::functions::pureToSqlQuery::mergeOldAliasToNewAlias': ca | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `functions::pureToSqlQuery::testReAliasMergedJoinOperations` | NotImplementedException: store resolution left user call 'meta::relational::functions::pureToSqlQuery::buildAn | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `functions::sqlstring::testEqualityInFilterOnOptionalPropertiesLegacy` | AssertFailed: assertEquals (sql-text, DB2 — text is the contract): expected select "root".FIRSTNAME as "name"  | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `functions::sqlstring::testIsDistinctSQLGeneration` | AssertFailed: assertSameSQL (sql-text, DB2 — text is the contract): expected select "root".LEGALNAME as "Legal | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `functions::sqlstring::testNotEqualityInFilterOnOptionalPropertiesLegacy` | AssertFailed: assertEquals (sql-text, DB2 — text is the contract): expected select "root".FIRSTNAME as "name"  | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `functions::sqlstring::testSqlGenerationDivide_AllDBs` | AssertFailed: assertSameSQL (sql-text, DB2 — text is the contract): expected select ((1.0 * "root".quantity) / | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `groupBy::testGroupByWithJoinDB2` | AssertFailed: assertEquals (sql-text, DB2 — text is the contract): expected select "root".LEGALNAME as "legalN | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `m2m2r::testProp3` | DialectCapability: LIST_PRODUCT reached a dialect without a list encoding | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `mapping::union::biTemporal::testBiTemporalUnionAsJoinTarget_correlatedSubqueryQuoting` | AssertFailed: Assert failed | asserts the engine's alias/quoting SPELLING ("unionalias_1"."lake_thru_0") — a regression test for an H2 case-sensitivity bug in the engine's printer; a text idiom |
| `mapping::union::biTemporal::testBiTemporalUnionJoin_milestoningColumnInOnClause` | AssertFailed: Assert failed | asserts the engine's alias/quoting SPELLING ("unionalias_1"."lake_thru_0") — a regression test for an H2 case-sensitivity bug in the engine's printer; a text idiom |
| `mapping::union::testChainedUnions` | AssertFailed: Assert failed | the ROW asserts pass; the failing assert is assert(sql->contains('union_gen_source_pk_0')) under the engine's union/join-removal execution option — the text idiom of an engine optimizer |
| `mapping::union::testEnumFilterWithUnionMappingPlanGeneration` | NotImplementedException: plan: alias 't2' not resolvable to a table (Subselect) | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `mapping::union::testProjectThroughAsso` | AssertFailed: Assert failed | the ROW asserts pass; the failing assert is assert(sql->contains('union_gen_source_pk_0')) under the engine's union/join-removal execution option — the text idiom of an engine optimizer |
| `mapping::union::testProjectThroughAssoWithJoinInMapping` | AssertFailed: Assert failed | the ROW asserts pass; the failing assert is assert(sql->contains('union_gen_source_pk_0')) under the engine's union/join-removal execution option — the text idiom of an engine optimizer |
| `mapping::union::testUnionWithSinglePropertyMapping` | AssertFailed: Assert failed | the ROW asserts pass; the failing assert is assert(sql->contains('union_gen_source_pk_0')) under the engine's union/join-removal execution option — the text idiom of an engine optimizer |
| `milestoning::applyMilestoningFilters::testMilestoningFilterApplicationOnSemiStructuredRelationalOperationElements` | TypeInferenceException: in function 'meta::relational::functions::pureToSqlQuery::reprocessAliases': expected  | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `postProcessor::filterPushDown::testPushFiltersDownToJoinsPostProcessorToSQL` | type: walled body 'meta::relational::functions::sqlQueryToString::sqlQueryToString': the engine's SQL printer  | walled printer body (DbConfig.selectSQLQueryProcessor / translation / post-processor / column rename) |
| `postProcessor::testDb2ColumnRename` | type: walled body 'meta::relational::functions::sqlQueryToString::sqlQueryToString': the engine's SQL printer  | walled printer body (DbConfig.selectSQLQueryProcessor / translation / post-processor / column rename) |
| `postProcessor::testPostProcessTransformJoinOp` | type: sqlQueryPostProcessorsConnectionAware hook shape is not a replaceTables lambda — post-processor recogniz | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `projection::filter::testFilterAfterJoinInRelationWithExtendedPrimitives` | AssertFailed: assertSameSQL (sql-text, oracle declined: golden execution: Syntax error in SQL statement "selec | the golden itself is malformed ('[*]' inside the SQL) — unjudgeable |
| `projection::view::testUnionOnViewsMapping` | AssertFailed: Assert failed | the ROW asserts pass; the failing assert is assert(sql->contains('union_gen_source_pk_0')) under the engine's union/join-removal execution option — the text idiom of an engine optimizer |
| `query::filter::isempty::testIsEmptyOnCollection` | AssertFailed: assertEquals (sql-text, oracle declined: plan-text unformatted (planToStringWithoutFormatting) — | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `query::legacyNullUnsafeEquals::testLegacyFlagRestoresOptionalParamFreeMarkerSelector` | AssertFailed: Assert failed | named deferral: the plan-template selector half of LEGACY_SQL_NULL_UNSAFE_EQUALS |
| `query::routing::multipleexpressions::testPlatformExpressionDependencyOnAFromExpression` | type: unknown function 'routeFunction' — no function of this name in the native or user catalog (unported plat | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `query::routing::multipleexpressions::testPlatformExpressionDependencyOnAFromExpression2` | type: unknown function 'routeFunction' — no function of this name in the native or user catalog (unported plat | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `query::routing::testCompositionInMultiStatementPureExpressions` | type: unknown function 'asString' — no function of this name in the native or user catalog (unported platform  | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `query::routing::testRoutingOfSimpleQualifiedProperty` | type: unknown function 'routeFunction' — no function of this name in the native or user catalog (unported plat | engine router machinery: routeFunction / byPassRouterInfo / asString / relation / newMultiValueMap |
| `query::take::testFilterLimitInSequenceForTableAccessor` | AssertFailed: assertEquals (sql-text, oracle declined: golden execution: Table "SELECTTOP1" not found; SQL sta | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `query::take::testLimitFilterInSequenceForTableAccessor` | AssertFailed: assertEquals (sql-text, oracle declined: golden execution: Table "SELECTTOP1" not found; SQL sta | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `runtime::extractDBs::testExtractDBsWithSubstituition` | NotImplementedException: store resolution left user call 'meta::relational::runtime::extractDBs' uninlined — t | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `tds::sort::testSortQuotes` | AssertFailed: assertEquals (sql-text, DB2 — text is the contract): expected select "root".FIRSTNAME as "name", | non-H2 dialect TEXT golden (DB2 / SQL Server spellings; text is the contract, rows unjudgeable) |
| `tds::window::routing::testExecutionPlanGeneration` | type: no overload of 'meta::pure::functions::relation::over' structurally matches the argument types (ExprType | full plan-TEXT equality (planToString / planToStringWithoutFormatting — PureExp / Sequence / Relational nodes): the engine's plan printer |
| `typeInference::testTranslateDbType` | NotImplementedException: scalar match: no arm statically accepts input type meta::relational::metamodel::datat | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |
| `meta::relational::transform::autogen::tests::testClassesAssociationsAndMappingFromDatabase` | type: unknown class 'meta::protocols::pure::vX_X_X::metamodel::PureModelContextData' in ^meta::protocols::pure | engine internals with no user surface: connection equality over extension arms / DDL / SQL-model transforms / alias merging / temp tables / reflection / protocol / plan-node instanceOf |


# Homework 2 — the burn and design rows, read to the mechanism (2026-09-12)

USER: "Let's do the homework for the burn and design ones? Then we get to internals". Every row
below: the test's assert, what the engine does (source read at 4.145.0), what we do (SQL dumped
or refusal read), and the decided shape.

## The remaining burns

| test | the mechanism | decided shape |
|---|---|---|
| `graphFetch::tests::simple::testCheckedWithCircularConstraints` | `graphFetchChecked` over `Person{firstName, firm{legalName}}`. Firm's constraint `duplicateEmployee` reads `$this.employees->isDistinct(#{…}#)` unless `type == CORPORATION`. The engine evaluates constraints against the FETCHED TREE only: for Firm X (not a corporation) `employees` is not in the tree, the generated checker throws, and the catch branch reports the defect `Unable to evaluate constraint [duplicateEmployee]: data not available - check your mappings` on the `firm` path. Ours evaluates the constraint against the STORE (the subquery over employees is available to us) and finds it holds — no defect. `CheckedEnvelope` already produces exactly that catch-branch defect for a NULL predicate. | **BURN (a real leg): constraint scope = the fetched tree.** A constraint's property reference outside the tree lowers to the "data not available" marker (NULL), so `A \|\| unfetched` short-circuits to TRUE when A holds and to the defect when it does not — the engine's left-to-right evaluate-or-throw, in SQL three-valued logic. |
| `mapping::relation::testMixedMappingWithFilterInProject` | `MixedMapping`: Person mapped through a RELATION FUNCTION (`~func …personFunctionWithProject`, a `+firmId` extra column) beside a Relational set; the projection navigates `$x.firm.employees->filter(age < 35).firstName`. Our union of Person's members needs the navigation key `firm_ID` and no member's projected row carries it — the relation-function member exposes `firmId` only through its `+firmId` extra column. | **DESIGN (union + relation-function mapping lane):** navigation keys must come from a relation-function member's extra columns. |
| `alloy::connections::tests::relationalMapper::testRelationalMapperTwoDBs` / `…WithJoin` | The connection carries `relationalMapperPostProcessor(^RelationalMapper(databaseMappers, schemaMappers, tableMappers))`; the SQL text is asserted. OUR renames are already applied (`snDB.productSchemaNewDBINC.productTableNewINC` ✓ — `RelationalMapperRenames.extract`). What differs is TEXT: the join alias is derived from the RENAMED table (`synonymtablenew_0`) where the engine keeps the ORIGINAL (`synonymtable_0`), and the mapping's filtered join spells a subselect with `is distinct from` where the engine inlines `"synonymtable_0".ID <> 1` in the ON clause. | **BURN (text idiom, small):** alias from the pre-rename name; the filtered-join spelling is shared with other goldens — check those before touching it. Low value: no rows are judged (the renamed schemas do not exist on H2). |

## The design rows, sorted by what they actually are

| test | what it is after reading | verdict |
|---|---|---|
| `executionPlan::m2m2r::tests::planGraphFetchWithDerivedProperty` / `…NestedDerivedProperty` | full PLAN TEXT of `StoreMappingGlobalGraphFetch` / `InMemoryRootGraphFetch` nodes over an M2M mapping | OUT (plan-text of in-memory nodes) — see the plan-text census below |
| `executionPlan::tests::execution::testPureExecutionStrategyFor…` ×2 | constructs `^Service(...)` instances and tests the execution STRATEGY chosen for plan nodes | OUT (engine plan machinery) |
| `executionPlan::tests::testModelConnectionJoin` | M2M chain (`ModelChainConnection`) plan text; our refusal is the M2M source class unmapped | DESIGN (M2M chain) |
| `graphFetch::tests::XStore::inMemoryAndRelational::testCrossMappingJsonToDBWithExplosion` | M2M explosion (a roadmap feature upstream too) | DESIGN (M2M) |
| `graphFetch::tests::XStore::inMemoryAndRelational::testCrossStoreWithCSVDataSource` | cross-store: a JSON/CSV in-memory source joined to relational | DESIGN (XStore) |
| `lineage::scanColumns::test::testNonDataTypeProperty` | the engine's LINEAGE program (scanColumns) — body not even a test of a query | OUT |
| `tds::schema::tests::resolveSchemaTest` | `assertSchemaRoundTripEquality` = the engine's `resolveSchema` program vs execution | OUT (PARKED: needs reflective equality) |
| `functions::sqlstring::testToSQLStringWithAbs` / `…WithAggregation` | `runTestCaseById('…')`: the engine's data-driven test REGISTRY (instances of a TestCase class, iterated) | OUT (instance-registry machinery); the underlying `toSQLString` asserts are ordinary text tests worth lifting out if ever needed |
| `json::testResultToJsonStream` | an in-memory `^TabularDataSet(columns, rows)` instance streamed to JSON | DESIGN (instances / code-as-data) |
| `mapping::include::testStoreSubstitution` | `mapping->resolveStore(store)` — the engine's mapping API over includes + store substitution; a fact we already hold as RELATIONS (metamodel-as-relations) | HIJACK CANDIDATE (see below) |
| `mapping::modelJoin::advanced::testNestedModelJoinCompoundInnerCondition` | model join, association unmapped in the nested mapping | DESIGN (model join) |
| `graphFetch::tests::union::propertyLevel::test6` | `special_union` (merge-by-join-name) member order | DESIGN (inclusive union) |
| `advanced::forced::structure::testQualifierWithOperation` ×2 | qualified-property results in operator runs (StoreLane provenance) | DESIGN (operator-run provenance) |

# Internals — can we "hijack" the engine's functions so its tests return OUR results?

The honest frame: the platform already does this in two places, and those two are the only
shapes of hijack that make sense.

1. **The plan-text channel.** `planToString` is the engine's own vocabulary and we already
   print single relational nodes, sequences and allocations in it, byte-exact. Every
   "plan-text" OUT row is a node kind or a shape this channel does not print yet. Extending it
   IS returning our result in the engine's clothes, and it is honest: the SQL inside is ours,
   the envelope is theirs. The census below measures how far each of the 19 rows is.
2. **Subsumed programs.** An engine PROGRAM whose value nothing consumes (createDbConfig,
   relationalExtensions) stays a typed opaque value and the test passes around it. Shrink-only,
   each with a written reason.

A third shape is new and worth a small leg: **engine API functions over the METAMODEL** —
`resolveStore(mapping, store)`, connection equality, `contextHasFlag(context, flag)`. Their
inputs are model facts we already hold as relations (metamodel-as-relations) or typed context
facts we already read (`ContextReading`). A platform native over those facts returns the
engine's answer from our data — "Java orchestrates, the database executes", no fabrication.
Rows: `testStoreSubstitution` (1), `testConnectionEquality*` (5), the 5
`useDbNativeImplicitNullOrdering` rows IF `contextHasFlag` unlocks the printer function's
body (uncertain — it is the printer's own config resolution).

What does NOT make sense, and why:

- **Router machinery** (`routeFunction`, `byPassRouterInfo`, …, 12 rows): the asserts inspect
  the ROUTED structure (RoutedValueSpecification shapes). Faking that structure would be a
  second router with no consumer — the definition of a stub.
- **Walled printer bodies** (15 rows): hand-built `SelectSQLQuery` instances printed through
  `sqlQueryToString`/`NullOrderingSupport`. Hijacking means an engine-SQL-metamodel → our IR
  bridge PLUS reproducing the printer's edge spellings (CASE shims for null ordering). High
  cost, no user surface, and every spelling we mirror is one we cannot later improve.
- **Protocol / lineage / DDL / SQL-model transforms** (19 rows): programs over the engine's
  own data structures with no user-visible result.
- **Non-H2 dialect text goldens** (9 rows): DB2 / SQL Server spellings. Implementable as
  dialect rows in the text renderer, but only worth it if a user targets those databases.

Recommendation: after the burns, one leg on the plan-text channel driven by the census, and one
small leg on the metamodel-API natives (`resolveStore` first — one row, a clean pattern).

## Plan-text census (19 rows run one by one, first difference read)

| row | how far | what it needs |
|---|---|---|
| `testGroupByWithOpenVariableInAgg`, `…TwoOpenVariablesInAggAndFilter` | NEAR: `else cast(0.0 as float)` vs our `else 0.0` inside the CASE under sum; and the calendar join emitted before the org-chart join where the engine joins org-chart first | two text spellings (float-literal cast in an aggregate CASE; calendar-aggregation join order) |
| `testIsEmptyOnCollection` | NEAR: `where (${collectionSize(input![])}) = 0` vs our `coalesce(len('${input…}'), 0) = 0` | one plan-template idiom: an optional COLLECTION parameter's isEmpty spells the engine's `collectionSize` template |
| `testTemporalDateVariableInFunctionExpressionWithPropagation` | MEDIUM: engine joins flat (`left outer join ProductClassificationTable … left outer join ProductExchangeTable`), ours isolates the milestoned navigation in a subselect | join form of a propagated milestoning variable |
| `testTwoMappingsOneRuntime`, `…WithoutExternalMapping` | MEDIUM: two mappings, one runtime — the engine nests the union of the two mappings' sets in a fixed alias order (`persontable_1/_3`), ours flattens the first set | the multi-mapping union plan shape |
| `withPlatform`, `testProp3` | NODE KIND: a `PureExp` node (a host expression such as `makeString` over a Relational child) — our text dialect refuses the collection reduction | the PureExp plan node |
| `testQuoteIdentifiersFlagWithGraphFetch`, `testMilestonedProperty` | NODE KIND: `StoreMappingGlobalGraphFetch` (graph-fetch plans) | the graph-fetch plan node family |
| `testEnumFilterWithUnionMappingPlanGeneration` | plan resultColumns over a union root: alias `t2` not resolvable to a table | union roots in the plan channel |
| `testViewToTDS` | relation root over a VIEW | the view relation (the same design as the accessor-on-view row) |
| `relationalResultSourcingOfListExecutionPlan` | computed scalar projection spelling pending | scalar-projection plan node |
| `testCrossDbPlanGenerationWithRelationFromWithOnlyRuntimes` | the mapping argument is a user call returning a mapping | plan-path argument shape |
| `testAlloyTestDatGenWithQuotedColumnsForViews` | test-data generation over a view | test-data-gen subsystem, not the plan channel |
| `testExecutionPlanGeneration` (tds window routing) | a TYPING refusal: `over` over wrapped functions (`function1/2/3`) | typer, not plan text |
| `testSupportStreamFlagWithGraphFetchAndFrom` | a TYPING refusal: a graph tree in a let binding has no type outside its call | typer |
| `inheritance`, `testModelConnectionDeepFunction` | mapping lanes (inheritance sets; the M2M chain) | DESIGN |

So the "hijack" of plan text that makes sense is REAL and measured: 5 rows are one or two
spellings away, 4 are one node kind away (PureExp ×2, graph-fetch ×2), the rest are lanes.
