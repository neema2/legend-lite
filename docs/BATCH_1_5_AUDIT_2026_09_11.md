# Batches 1–5 of the upstream boundary program — the audit (2026-09-11)

USER: "Do full audit of phases 1-5 then hopefully on to 6." Each batch's DONE-CRITERION
(docs/UPSTREAM_BOUNDARY_PROGRAM.md §4) was re-measured against the tree at c0f792c0f,
not against the landing records. Verdicts: **HOLDS** (measured true now), **DEFERRED**
(claimed for the batch, not done — named, with the fix), **SHORTCUT** (done in a way the
program did not mean). Every measurement is a command a fresh session can rerun.

## Batch 1 — one release

| claim | measurement | verdict |
|---|---|---|
| `version-report.sh --check` exits 0 | ran: exit 0; pins 4.138.2 / 5.92.0; both checkouts on the tags (`git describe --exact-match`) | HOLDS |
| `classpath-convergence.sh` reports 0 divergent, boundary clean | ran: shared 136, divergent 0 (legend 0, third-party 0); core 0 legend artifacts | HOLDS |
| `--check` runs in CI | `.github/actions/gate-env/action.yml` runs it after cloning both oracles at the pinned SHAs | HOLDS |
| convergence runs in CI "on a pin change" (§5) | it ran nowhere in CI (3.5 s locally) | DEFERRED → **this leg**: a gate-env step beside `--check` |

## Batch 2 — loud, not silent

| claim | measurement | verdict |
|---|---|---|
| path manifest test, every upstream path named | `UpstreamPathManifestTest`, PINNED_COUNT 90 (91 after this leg), misses listed | HOLDS |
| the 4 silent `continue`s reported | `Corpus.java`'s 7 `continue`s are lexer-loop steps; `MinimalCorpus`'s 3 are a recorded skip (engine-implementation key), a same-text dedupe, and a recorded missing input | HOLDS |
| census precheck 1 → 9 roots | `SpecBodyCensusTest` prechecks every `PLATFORM_ROOTS` entry, loud on a miss | HOLDS |
| fixture version inside the file, asserted | `engine-grammar-fixtures-4.138.2.jsonl` line 1 is `# engine=4.138.2`; `Corpus` (parser-equivalence) resolves the file by `OraclePins.engineRelease()` | HOLDS |
| keyword-snapshot shrink direction | `SurfaceCensusTest` fails on rows the engine no longer defines | HOLDS |

## Batch 3 — claims

| claim | measurement | verdict |
|---|---|---|
| every overload claimed; UNCLAIMED shrink-only | `ClaimRegistryTest.UNCLAIMED_MAX = 0`; ledger current (regenerated in legs A, C, D) | HOLDS |
| no hand table in `Claims` | zero `Map.of`/`List.of("` in Claims.java; three string-switch families are closed enums | HOLDS |
| `KNOWN_ABSENT` deleted | zero references in main | HOLDS |

## Batch 4 — membership

| claim | measurement | verdict |
|---|---|---|
| unclaimed entries left Pure.java | UNCLAIMED 0; 4 rows no handler names (`replaceTreeNode`, `executeHTTPRaw`, `mutateAdd`, `addColumns`) are WALLS — loud refusals with reasons | HOLDS |
| prelude carries respelled natives; exclusion keys on claims | 37 `native function` declarations in prelude.pure; the generator's rule reads `Claims.claimedBareNames()` | HOLDS |
| §6.2 "the prelude carries ALL upstream natives in the read roots" | `tools/native-axes.py`: 286 upstream natives; 74 not in Pure.java; 33 of those in the prelude; **41 in NEITHER** — 39 are engine-root natives (`core_functions_unclassified`: `newClass`, `parseCSV`, `encodeUrl`, `readFile`, …; `json.pure`; the compiler functions), 1 is the CoreFn form `new` (correct), 1 is `meta::legend::execute` (bare-name collision with the router's `execute`, correct). The generator reads FUNCTIONS from legend-pure's platform roots only (USER 2026-09-09: "the platform packages WHOLE"); the engine roots are read for classes. So "unknown function" is still the answer for 39 engine natives | DEFERRED → **next leg**: engine-root natives respelled into the prelude (declarations only; measured by the chain) |

## Batch 5 — generated text

| claim | measurement | verdict |
|---|---|---|
| signature text verified then generated; same-FQN-different-signature → 0 | `NativeSignatureGeneratorTest`, DIVERGENT_MAX 0, 752 membership rows byte-identical | HOLDS |
| `CORE_IMPORTS` generated from m3.pure, compared as a SEQUENCE | a hand list in `NameResolver`; NOT compared. Measured against the checkout: upstream has TWO lists — pure's `coreImport` (29) and the engine's `CompileContext.META_IMPORTS` (32 = those 29 + variant/relation/precisePrimitives INTERLEAVED); ours was the 29 with the three APPENDED — a different first-match order from the engine's, whose code the corpus is | DEFERRED → **this leg**: `CoreImportsParityTest` holds ours equal to the engine's sequence and pure's equal to that set minus three; `-Dimports.generate=1`; the list reordered to the engine's |
| `PlatformTypes` spellings generated; drift test deleted; 7 inline literals → 0 | `PlatformTypesDriftTest` still pinned 6 of 127 by containment in our own prelude; 7 `meta::pure::metamodel::type::Any` literals still inline (+1 `metamodel::Package`) | DEFERRED → **this leg**: `PlatformNamesSpellingTest` holds every one of the 141 spellings against the checkouts' declarations (pure whole + the engine spec roots + m3.pure's bootstrap instances) or the system metamodel; the drift test is deleted; the 8 literals read the constant. "Generate" is moot: the spelling IS the FQN — existence is the only derivable fact |
| the 104 `SystemMetamodel` spellings folded into a generated table | the prelude generator already demands every name the system metamodel's source uses (`systemDemand` → the prelude closure) — an unknown spelling fails generation parity | HOLDS (by a different mechanism than the plan said) |

**Finding 5.4 — a spelling the audit could not verify against upstream.** m3.pure's
un-annotated bootstrap declarations (the `Package` class and the primitive types) sit at
upstream's ROOT (`M3Paths.Package = "Package"`, `M3Paths.String = "String"`). The prelude
generator RE-HOMES them under `meta::pure::metamodel` (Package) and
`meta::pure::metamodel::type` (primitives), and the whole catalog — every signature's
`meta::pure::metamodel::type::String[1]` — is spelled that way. It is the platform's
canonical spelling, consistent everywhere, and moving it is a program decision (§6), not
an audit fix; the spelling test applies the generator's rule so the constant is held
against the same derivation. Recorded, not changed.

## The thesis's four sentences (§1), measured

1. **One release, one file, enforced in CI** — HOLDS (`oracle-pins.env`; `--check` in gate-env).
2. **core has zero upstream deps; every upstream fact generated and asserted** — deps: HOLDS (convergence: 0 artifacts). Facts: prelude, signatures, membership, claims, DynaFn, CORE_IMPORTS (this leg), PlatformTypes spellings (this leg) are asserted every gate-1 run with the checkouts present (CI clones them). The Maven enforcer ban and ArchUnit import ban the plan names for `core/pom.xml` are batch 7's (the `spec` module); the measurement stands in for them today.
3. **Pure.java is exactly the implemented surface** — HOLDS (UNCLAIMED 0; DIVERGENT 0; the batch-5 audit's four legs).
4. **Everything that reads upstream lives outside core** — NOT YET: the readers are `core/src/test` (rcorpus, the generators, the parity tests). Batch 7 relocates them; nothing in `core/src/main` reads a checkout (ArchitectureTest + convergence).

## What this leg landed (batch 5 remainder)

- `CoreImportsParityTest` + `NameResolver.CORE_IMPORTS` in the engine's order (+1 manifest path, PINNED_COUNT 91).
- `PlatformNamesSpellingTest`; `PlatformTypesDriftTest` deleted; 8 inline metaclass literals → `PlatformTypes` constants.
- `classpath-convergence.sh` in the CI gate environment beside `--check`.

## Deferred to the next leg

- The 39 engine-root natives respelled into the prelude (batch 4 §6.2).
