<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.legend</groupId>
        <artifactId>legend-lite</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>legend-lite-core</artifactId>
    <packaging>jar</packaging>
    <name>Legend Lite - Core</name>

    <properties>
        <!-- heavy (corpus) tests stay out of the unit gate; gate 4 runs them
             explicitly with -Dsurefire.excludedGroups= -->
        <surefire.excludedGroups>heavy</surefire.excludedGroups>
    </properties>
    <description>
        Clean reimplementation of the Legend Lite compiler pipeline
        (text → SQL). Strangler Fig sibling to legend-lite-engine.
        See README.md — the wall is enforced by ArchitectureTest.
    </description>

    <dependencies>
        <!-- JDBC drivers — only because the SQL renderer targets these dialects.
             Tests may exec SQL through them. No engine/ types may leak through. -->
        <dependency>
            <groupId>org.duckdb</groupId>
            <artifactId>duckdb_jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
        </dependency>
        <!-- H2 backend (H2_BACKEND.md §11-12): the engine's own reference
             target. Version pinned to the ENGINE's (its goldens ran on a
             forked 2.1.214); DbMetaData already required this driver at
             runtime via reflection — now an honest compile dependency. -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>2.1.214</version>
        </dependency>

        <!-- Test deps -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- THE NULL GATE (AUDIT_PROGRAM §3): NullAway via Error Prone,
                 build tooling only — annotationProcessorPaths never touches
                 the compile/runtime classpath or the shipped jar. Production
                 dependencies stay the two JDBC drivers.

                 The AnnotatedPackages list below IS the adoption ratchet:
                 a listed package builds under the non-null-by-default rule
                 (fields/params/returns; @com.legend.Nullable declares the
                 exceptions) and NEVER leaves the list. Adoption order
                 (cheapest proof first): sql -> lowering -> compiler/spec ->
                 resolver; normalizer/exec/harness AFTER their T4.1/T4.2/V3.1
                 restructures — do not annotate code scheduled for deletion.

                 MAIN sources only (default-compile) — MEASURED decision, not
                 an oversight (NULL_GATE_VERIFICATION §6.2, re-verified
                 2026-07-30): gating tests surfaced 286 findings of which
                 ~123 were getMessage() assertion derefs (now hardened via
                 String.valueOf) and the rest are the test idiom itself —
                 fixture derefs ARE the assertions, dummy builders pass null
                 deliberately. The known test-only signature holes (G3.1:
                 Multiplicity.range, Ddl schema, compileModel) are closed
                 in main. Test code otherwise keeps its
                 looser discipline until the gate has proven itself.

                 The forked-compiler -J flags below are the - -add-exports
                 block Error Prone needs on JDK 16+ (load-bearing build
                 config; expect to touch it on any JDK upgrade). -->
            <!-- Heavy (stress/benchmark) tests run only when asked:
                 mvn test -Dgroups=heavy -Dsurefire.excludedGroups= -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludedGroups>${surefire.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
            <!-- The server entrypoint (moved from the deleted engine module):
                 `mvn -pl core exec:java` runs the HTTP server; `package`
                 produces the shaded fat jar. -->
            <!-- THE UPSTREAM BOUNDARY, ENFORCED (batch 7c, 2026-09-11): this module
                 depends on NO artifact of the engine's or pure's groups, in any
                 scope — the checkouts are read as files by the spec module, the
                 oracle jars live only in pct and parser-equivalence. Our own
                 artifacts are com.legend (renamed from the squatted org.finos.legend
                 for exactly this rule). The build fails, not a test. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>enforce-upstream-boundary</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>org.finos.legend:*</exclude>
                                        <exclude>org.finos.legend.*:*</exclude>
                                    </excludes>
                                    <message>the upstream boundary: no org.finos.legend artifact may reach this module (docs/UPSTREAM_BOUNDARY_PROGRAM.md workstream B)</message>
                                </bannedDependencies>
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- core's TEST jar (batch 7b): the spec module reads the claims
                 registry and the test-side helpers from it -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.legend.server.LegendHttpServer</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>shaded</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.legend.server.LegendHttpServer</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <configuration>
                            <fork>true</fork>
                            <compilerArgs>
                                <arg>-XDcompilePolicy=simple</arg>
                                <arg>-Xmaxerrs</arg>
                                <arg>10000</arg>
                                <arg>--should-stop=ifError=FLOW</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>
                                <arg>-J--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>
                                <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                                <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>
                                <arg>-Xplugin:ErrorProne -XepDisableAllChecks -Xep:NullAway:ERROR -XepOpt:NullAway:AnnotatedPackages=com.legend -XepOpt:NullAway:CustomNullableAnnotations=com.legend.Nullable -XepOpt:NullAway:CustomNonnullAnnotations=com.legend.NonNull -XepOpt:NullAway:JSpecifyMode=true -XepOpt:NullAway:CheckOptionalEmptiness=true</arg>
                                <!-- JSpecify mode on JDK21 REQUIRES this arg; without it
                                     NullAway 0.13.x hard-crashes (NULL_GATE_VERIFICATION G3) -->
                                <arg>-XDaddTypeAnnotationsToSymbol=true</arg>
                            </compilerArgs>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>com.google.errorprone</groupId>
                                    <artifactId>error_prone_core</artifactId>
                                    <version>2.50.0</version>
                                </path>
                                <path>
                                    <groupId>com.uber.nullaway</groupId>
                                    <artifactId>nullaway</artifactId>
                                    <version>0.13.8</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
